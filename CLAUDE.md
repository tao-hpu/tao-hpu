# tao-hpu/tao-hpu（GitHub 首页 README）

这个仓库只有 `README.md`，它就是 github.com/tao-hpu 的首页。没有构建，push 即生效。

## Selected contributions 段要和个人站同步

同步规则与个人站 `tao-hpu.github.io` 共享一份（两个仓库指向同一个文件，改一处两边生效）：

@~/.claude/shared/contributions-sync.md

## 其他注意

- 标题 `### 🌱 Selected contributions` 的文字被个人站当锚点用，改之前看上面那份规则。
- star 数一类会过期的数字不要写死，用 `img.shields.io/github/stars/<owner>/<repo>` 徽章。
- 只写已合并的上游 PR。开着的 PR 不进 README，合了再补。
